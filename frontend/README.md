# VNDLY CWM — Frontend

React + TypeScript + Tailwind CSS + React Router + Axios + TanStack Query.

See the top-level `README.md` (one directory up) for full setup instructions,
API documentation, seed credentials, and end-to-end testing steps.

Quick start:

```bash
npm install
cp .env.example .env
npm run dev
```
